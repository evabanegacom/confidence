<section class='section'>
  <div class='container'>
    <div class='columns'>
    <% if user_signed_in?%>
      <%#= render 'profile' %>
    <%= render 'who_to_follow' %>
    <%#= render 'attendees' %>
    <% else %>
      <%= render 'trends' %>
    <% end %>
      <%= render 'feed' %>
</div>
  </div>
</section>